extends tableLayout

block content
    table
        thead 
            tr 
                th ID
                th Name 
                th Price
        tbody 
            each row in data
                tr
                    td #{row.id }
                    td #{row.name}
                    td #{row.price}
