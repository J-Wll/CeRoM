extends layout

block content
  h1= title
  p Welcome to #{title}
  ol 
    each val in data
      li #[strong ID:] #{val.id} #[strong NAME:] #{val.name} #[strong PRICE:] £#{val.price}
    strong test


  table
    thead 
      tr 
        th ID
        th Name 
        th Price
    tbody 
      each row in data
        tr
          td #{row.id }
          td #{row.name}
          td #{row.price}
